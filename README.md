# devops-practice

This simply creates a python application, runs tests and deploys it if the tests are passed
All this a done using the Github's VM which is automatically deleted after completion of tasks
