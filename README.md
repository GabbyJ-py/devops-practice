# devops-practice

This simply creates a python application, runs tests, deploys and uploads the artifact if the tests are passed.
All this are done using the Github's VM, which is automatically deleted after completion of tasks.
